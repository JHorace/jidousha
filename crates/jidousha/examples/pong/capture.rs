//! placeholder
use jidousha::prelude::*;
use jidousha::testing::FrameRecord;
/// placeholder
pub(crate) fn write_the_picture(_f: &FrameRecord, _c: &Camera) -> Result<String, String> {
    Ok("none".into())
}
